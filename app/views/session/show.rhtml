<b><%= @a_session.details %></b>

<% for column in Session.content_columns %>
<p>
  <b><%= column.human_name %>:</b> <%=h @a_session.send(column.name) %>
</p>
<% end %>

Residents: <br />

<% if @a_session.residents.count > 0 %>
  <table>
  
  <% @a_session.residents.each do |resident| %>
    <tr>
      <td><%= resident.name %></td>

      <!--td --><!-- %= link_to 'Remove', { :action => 'remove_resident', 
                :id => @session.id, :resident_id => resident.id}, 
                :confirm => 'Are you sure?', 
                :method => :post %>
      </td-->
      <td>
        <%= button_to "Remove", { :action => 'remove_resident',
                :id => @a_session.id, :resident_id => resident.id },
                :confirm => "Are you sure?", :method => :post %>
      </td>

  <% end %>


    <tr>
  <table>

<% end %>

<br />
<br />

<%= link_to 'Add Resident', :action => "add_resident", :id => @a_session.id %><br />

<%= link_to 'Edit', :action => 'edit', :id => @a_session %> |
<%= link_to 'Back', :action => 'list' %>
