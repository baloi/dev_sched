> install specific gem version:
  $ gem install rails -v 1.2.6
> rake rails:freeze:gems problem
  1. Gemrunner problem, edit 
    ~/.rvm/gems/ruby{version}/gems/rails-1.2.6/lib/tasks/framework.rake
  
    require 'rubygems'
    #Gem.manage_gems
    require 'rubygems/gem_runner'
  2. version update problem when "rake rails:freeze:gems"
    $ gem install rubygems-update -v 1.4.2
    $ gem uninstall rubygems-update -v {newer_version_like_1.5.0}
    $ update_rubygems
  3. run:
    $ rm -drf vendor/rails
    $ rake rails:freeze:gems 

> install rake task for automatically generating a gems file from your
  Rails gem dependencies. Rerun the rake task whenever you change your
  dependencies.
  $ curl https://gist.github.com/101101.txt > lib/tasks/heroku.rake
  $ rake heroku:gems
  $ git add lib/tasks/heroku.rake .gems 
  $ git commit lib/tasks/heroku.rake .gems -m 
    "Adding gem manifest for Heroku."
> Heroku
  gem install heroku
  heroku keys:add
  heroku create

> runing autotest

  $ autoest -rails

> open three terminal tabs
  ~/dev_barn, ~/barn, midnight-runner

> to develop go to ~/dev_barn:
    . git commit, fossil commit
  then go to ~/barn
    . always end day with 
      cd ~/barn 
      fossil update
  
   to use go to ~/barn

> set git configurations
  git config --global core.editor "vim"
